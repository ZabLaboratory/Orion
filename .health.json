{
  "service": "orion",
  "port": 4007,
  "gateway_prefix": "/orion",
  "liveness": "GET /api/v1/health",
  "readiness": "GET /api/v1/ready",
  "checks": [
    {
      "name": "http_health",
      "endpoint": "GET /api/v1/health",
      "expect_status": 200,
      "severity": "critical"
    },
    {
      "name": "gateway_smoke",
      "endpoint": "GET /orion/api/v1/health",
      "via": "zabgate",
      "expect_status": 200,
      "severity": "high"
    }
  ],
  "status": "production",
  "notes": "Orion v2 (Go) in production. Stateless since #15/#331 (ADR-BLUE-012 §4.3): no store, no ServiceTokenManager — /ready always answers 200 with the in-memory show roster count. The prior `service_token` check (ADR ZabAuth 003 Am.3 § A3.6 R21 / RC 51) is retired with it."
}
