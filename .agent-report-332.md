AGENT_REPORT

Role: forge
Agent-Thread: B3-R6-11-ORION-PROVIDERS
Work-Unit: B3-R6-11-ORION-PROVIDERS
Repo: ZabLaboratory/Orion
Issue: 332
Branch: forge/332-orion-providers
PR: https://github.com/ZabLaboratory/Orion/pull/348
SHA: 38aa35b31f790fbb939746100ff6a79defe45fc5

Result: READY

Summary:
- internal/providers: Registry() (core.http.request, show.emit, overlay-app
  blue.capability-provider.v1 descriptors), Policy(httpEgressAllowed)
  CapabilityPolicy, canonical.go (local LSML canonicalization, needed
  since blueruntime doesn't export it), BuildEvent/InjectActive (active-only
  Zab event injection adapter, library-only — no live transport wired).
- internal/api/scene_intent.go: SceneIntentDeps.Providers/Policy fields;
  Prepare/Take (lines 243,250) now pass them instead of nil, nil.
- cmd/orion/scene_intent_wiring.go: wires providers.Registry()/Policy(...)
  gated on cfg.HTTPEgressAllowHosts.
- New fixture internal/bluespike/testdata/02-http-requires.program.json
  (requires on core.http.request/1/request, digest verified against real
  ParseProgram).

Criteria -> evidence:
- E2E before/after (requires-program: fail without wiring, succeed with) ->
  internal/api/scene_intent_providers_test.go (3 tests: nil->500, wired
  true->200, wired false->500).
- Isolation §5.10 (on-air-only provider never fires in preview) ->
  internal/providers/isolation_test.go.
- Negative internal/store scan -> none (package removed by #331/#346;
  2 pre-existing comment hits in public.go only).
- go build/vet/test ./... -> all green (one internal/bluewire test flaky
  by timing, reproduced clean on rerun x3, untouched by this PR).

CI: not yet observed post-push (PR just opened).

Risks / hypothèses (also in PR body):
- Capability naming (core.http.request/show.emit/overlay-app, v1) is my own
  choice — no Blue compiler emits requires/effects for these yet (checked:
  ADR-BLUE-012 lists #135/#155/#158/#283 still open). If Blue later emits
  real requires, the tuple must match exactly or effectTypesMatch refuses
  admission even with a healthy provider present.
- Zab event injection (BuildEvent/InjectActive) is a tested library with no
  wired transport — no live Quasar consumer exists against bluehost yet;
  building one was judged out of this issue's explicit scope.

Rollback: revert both commits on forge/332-orion-providers (or the merge
commit once merged) — additive only, no schema/migration/deploy change.
