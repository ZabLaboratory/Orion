AGENT_REPORT

Agent-Role: keeper
Agent-Thread: ZAB-CI-B1-PG16-20260811
Work-Unit: ZAB-CI-B1-PG16-20260811
Issue: ZabLaboratory/Orion#317
ADR-Revision: sha256:2562685ce5459ff28505028f2eadaf96618363484108ff3b8a8a88d6ccf04e3a
Status: READY

Résultat : couche statique (docker-compose.yml, 3 replicas) basculée de zab-org-runner:pg
(PG12) vers zab-org-runner:pg16. JIT (.env RUNNER_IMAGE) et le build de l'image pg16
étaient déjà en place depuis une tentative partielle du 2026-08-05 ; ce cycle a fermé le
dernier écart (couche statique) trouvé par le scan Bastion.

Matrice critère → preuve :
- pg_lsclusters (--entrypoint) une seule ligne, ver 16 → `docker run --rm --entrypoint
  pg_lsclusters zab-org-runner:pg16` = `16 main 5432 down postgres ...` (une ligne).
- Label org.opencontainers.image.revision non vide → `docker image inspect
  zab-org-runner:pg16 --format '{{index .Config.Labels
  "org.opencontainers.image.revision"}}'` = `af78d5d9d0bdf8650c84cba9fc45849448106522`.
- RUNNER_IMAGE référence l'image en service + backup daté →
  `/home/ubuntu/runner-orchestrator-zab/.env` = `RUNNER_IMAGE=zab-org-runner:pg16` ;
  `.env.bak-keeper-pg16-20260805-105309` présent.
- Job non-DB vert après bascule, pas de queued>10min → PR ZabLaboratory/Orion#323
  (docs only) : 6/8 checks verts sur le pool fraîchement basculé (golangci-lint,
  govulncheck, secret-scan, conformance-matrix gate, exec-port-parity gate, perf-20k
  gate) ; build-test/staticcheck `in_progress` (non `queued`) au dernier poll. Fenêtre
  continue de 2h non couvrable dans cette session — résiduel, à surveiller si régression
  suspectée.

Commandes exécutées (VPS vps-ovh) :
- `cp Dockerfile Dockerfile.bak.20260811-010215` (backup pré-édit)
- `cp Dockerfile.pg16 Dockerfile.pg16.bak.<ts>` puis ajout `LABEL
  org.opencontainers.image.revision=...` après `FROM myoung34/github-runner:ubuntu-jammy`
- `cp Dockerfile.pg16 Dockerfile` (promotion, base jammy — focal-pgdg n'existe plus pour
  PG16, déjà documenté dans le draft du 2026-08-05)
- `docker build --load -t zab-org-runner:pg16 -f Dockerfile .`
- `cp docker-compose.yml docker-compose.yml.bak.<ts>` ; `image: zab-org-runner:pg` →
  `zab-org-runner:pg16`
- `./up.sh` (pas `docker compose up -d` nu — `APP_PRIVATE_KEY` non exporté, crash-loop
  constaté et corrigé en route)

Artefacts : docs/runbooks/ci-runner-pg-image.md mis à jour (section migration PG16),
commit 7d850024c399f79bcea369ef818aee10727d677e, branche
keeper/317-pin-pg16-runner-image, PR ZabLaboratory/Orion#323 (pure doc, ouverte, non
mergée — décision Eleven).

Exclusions respectées : aucun changement de workflow, aucun versionnement git du
Dockerfile VPS (hors scope B3/#321), aucun changement RUNNER_MEM/CPUS/CONCURRENCY, aucun
montage socket Docker.

Risques résiduels : aucun nouveau ; rollback documenté dans le runbook
(Dockerfile.bak.<date> + docker-compose.yml.bak.<date> + ./up.sh).
