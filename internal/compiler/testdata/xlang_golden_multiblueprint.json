{
  "_comment": "Cross-language LSML hash-parity golden for the N-blueprint envelope (ADR 001 criterion 7, extends issue #18). The layout binds two components to TWO distinct blueprint keys via the '<key>.<leaf>' path form (score.value / timer.value) — the wire shape the multi-blueprint envelope produces. PLACEHOLDER: `ts_canonical` and `ts_hash` MUST be regenerated by the real TS @lumencast/compiler (hashBundle()+canonicalize()) against the equivalent bundle and committed BEFORE merge (criterion 7 / R2). Until then they are the empty string and TestEmitLSML_CrossLanguageHashParity_MultiBlueprint FAILS — it must not go green on a fabricated Go-minted value. Orion's lsml.HashBundle of this layout is asserted self-consistent and deterministic regardless.",
  "scene_id": "xlang-multiblueprint-scene",
  "score_binding": "score.value",
  "timer_binding": "timer.value",
  "ts_canonical": "",
  "ts_hash": ""
}
